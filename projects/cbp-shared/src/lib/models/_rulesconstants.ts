export enum rulesConstants{
  IF='IF',
  ELSE='ELSE',
  THEN='THEN',
  AND='AND',
  OR='OR',
  IF_THEN='IF THEN',
  IF_THEN_ELSE='IF THEN ELSE',
  WHILE='WHILE',
  REPEAT='REPEAT',
  TIMED='TIMED',
  goto='goto'
}